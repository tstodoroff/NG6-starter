import angular from 'angular';

import mazeStartInputComponent from './mazeStartInput.component';

export default angular
  .module('app', [])
  .component('mazeStartInput', mazeStartInputComponent);
